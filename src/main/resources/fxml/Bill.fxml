<?import javafx.scene.chart.PieChart?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.BorderPane?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.Region?>
<BorderPane xmlns:fx="http://javafx.com/fxml"
            fx:controller="com.ledgerfx.controller.BillController"
            stylesheets="@/css/style.css" prefWidth="900" prefHeight="600">
    <top>
        <HBox spacing="15" alignment="CENTER_LEFT" style="-fx-padding: 10;">
            <ComboBox fx:id="categoryComboBox" promptText="分类" maxWidth="100"/>
            <TextField fx:id="amountField" promptText="金额" maxWidth="100"/>
            <ComboBox fx:id="typeComboBox" promptText="类型" maxWidth="100"/>
            <TextField fx:id="noteField" promptText="备注"/>
            <Button text="添加账单" onAction="#handleAddBill"/>
            <Button text="分析账单" onAction="#handleGoAnalysis"/>
            <Button text="导出 CSV" onAction="#handleExportCsv"/>
            <Region HBox.hgrow="ALWAYS"/> <!-- 分隔符，右侧控件靠右 -->
            <!-- 退出按钮 -->
            <Button onAction="#handleExit" text="退出" styleClass="icon-button"/>
        </HBox>
    </top>
    <center>
        <TableView fx:id="billTable">
            <columns>
                <TableColumn text="分类" style="-fx-alignment: center" fx:id="accountTypeCol"/>
                <TableColumn text="金额" style="-fx-alignment: center" fx:id="amountCol"/>
                <TableColumn text="类型" style="-fx-alignment: center" fx:id="typeCol"/>
                <TableColumn text="备注" style="-fx-alignment: center" fx:id="descriptionCol"/>
                <TableColumn text="时间" style="-fx-alignment: center" fx:id="timeCol"/>
            </columns>
        </TableView>
    </center>
    <right>
        <PieChart fx:id="categoryChart"/>
    </right>
</BorderPane>
